<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit message</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet"/>
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background-color: #e5ddd5;
      padding: 30px;
    }

    .edit-container {
      max-width: 600px;
      margin: auto;
      background-color: #fff;
      padding: 25px;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    h3, p {
      margin-bottom: 15px;
      color: #075e54;
    }

    .info-text {
      font-size: 14px;
      color: #555;
      margin-bottom: 20px;
    }

    label {
      font-weight: bold;
      display: block;
      margin-bottom: 5px;
      color: #333;
    }

    textarea {
      width: 100%;
      padding: 10px;
      font-size: 14px;
      border-radius: 6px;
      border: 1px solid #ccc;
    }

    button {
      margin-top: 20px;
      background-color: #075e54;
      color: white;
      border: none;
      padding: 10px 20px;
      font-size: 15px;
      border-radius: 5px;
      cursor: pointer;
    }

    button:hover {
      background-color: #0b7a65;
    }
  </style>
</head>
<body>

  <div class="edit-container">
    <p>Edit your message ID: <%= chat._id %></p>
    <h3>This chat was sent from <%= chat.from %> to <%= chat.to %></h3>
    <p class="info-text">Modify the message below and click <strong>"Save Message"</strong> to apply your changes.</p>

    <form method="post" action="/chats/<%= chat._id %>?_method=PATCH">
      <label>Message:</label>
      <textarea name="message" rows="8" cols="40" placeholder="enter your message here"><%= chat.message %></textarea>
      <button>Save Message</button>
    </form>
  </div>

</body>
</html>
